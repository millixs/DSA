# Use Ctrl+Shif+B to run the code through the terminal
# Input the values in input.txt
# Output will be displayed in output.txt

#include <bits/stdc++.h>
using namespace std;
int main (){
  string a;
  cin >> a;
  cout << "Hey: " << a << endl;
  return 0;
}

